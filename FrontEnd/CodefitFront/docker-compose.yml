version: "3"
services:
  react:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true
    command: sh -c "npm install -g create-react-app && create-react-app . && npm start"